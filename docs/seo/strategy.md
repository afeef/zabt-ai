# zabt.ai SEO/AEO/GEO Strategy

Positioning angles → topic clusters → page targets.

## Angles
1. **Self-hosted alternative** — "self-hosted Otter.ai alternative", "open source Fireflies alternative".
2. **Data privacy / compliance** — "private meeting transcription", "on-premise meeting notes", "HIPAA meeting transcription".
3. **Generic AI meeting notes** — "AI meeting notes", "meeting transcription software", "AI meeting summarizer".
4. **Open-source / developer** — "open source meeting transcription", "self-host meeting notes Docker", "Whisper meeting notes".

## Clusters → pages
- Comparison cluster → `/vs/otter`, `/vs/fireflies`, `/vs/fathom`, `/vs/tldv`, `/vs/read-ai`, `/vs/granola` (Phase 2).
- Privacy cluster → `/self-hosted`, `/private` (Phase 2).
- Use-case cluster → from `content/landing.ts` use cases (Phase 2).
- FAQ hub → expands `docs/faq.md` + pricing `faqItems` (Phase 2).

## Notes
Volume/difficulty figures land once DataForSEO is provisioned (see `tooling.md`).
