# GPU Worker Configuration
MODE=local                    # "runpod" or "local"
WHISPER_MODEL=large-v3
DIARIZATION_MODEL=pyannote/speaker-diarization-3.1
HF_TOKEN=hf_xxx               # Required for diarization + MedASR (gated model)
COST_PER_MINUTE=0.0046
PORT=8001

# Sentry (optional)
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
