# Implementation Plan: Zabt Rebranding & Logto Removal

**Branch**: `001-rebrand-zabt` | **Date**: 2026-02-22 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/001-rebrand-zabt/spec.md`

---

## Summary

Rename all occurrences of "Pareto", "Pareto AI", "Meetily", and any remaining "Logto" references across all tracked files in the monorepo to use "Zabt" / "zabt" consistently. Simultaneously rename the Postgres database from `pareto_ai` to `zabt`. No functional changes are introduced — this is a pure find-and-replace and dead-code cleanup.

---

## Technical Context

**Language/Version**: Python 3.11 (backend), TypeScript / Next.js 16 (frontend)
**Primary Dependencies**: FastAPI, Supabase JS SSR, pydantic-settings
**Storage**: PostgreSQL (via Docker Compose) — database name changing from `pareto_ai` → `zabt`
**Testing**: Playwright/Python (E2E) — rename verification via grep assertions
**Target Platform**: Linux (Docker Compose local dev)
**Project Type**: Web application (backend/ + frontend-2/)
**Performance Goals**: N/A — no runtime changes
**Constraints**: Must not alter any business logic; only string literals, config values, and package metadata
**Scale/Scope**: ~10 files across backend, frontend, docker config, and spec documents

---

## Constitution Check

| Gate | Applicable? | Status | Notes |
|------|-------------|--------|-------|
| Design System — UI compliance | Yes | Pass | Only brand name string changes in login/register UI; existing design tokens unchanged |
| API Contract — contracts/ populated | No | N/A | No new or modified endpoints |
| Auth/Security — no hardcoded secrets | Yes | Pass | No secrets introduced; env vars remain in `.env` |
| Env Config — vars in quickstart.md | Yes | Pass | DB rename documented in `quickstart.md` |
| Scope Boundary — within spec | Yes | Pass | Strictly rename only; zero functional changes |
| E2E Testing — Playwright/Python in tests/e2e/ | Yes | Pass | Verification test will grep for absence of "pareto"/"logto" |

---

## Project Structure

### Documentation (this feature)

```text
specs/001-rebrand-zabt/
├── plan.md              ← this file
├── research.md          ← Phase 0 complete
├── data-model.md        ← Phase 1 complete
├── quickstart.md        ← Phase 1 complete
└── tasks.md             ← generated by /speckit.tasks
```

### Files to Modify

```text
# Infrastructure & Config
.env                                        ← POSTGRES_DB: pareto_ai → zabt
docker-compose.yml                          ← DB name in comments/defaults
backend/app/core/config.py                  ← PROJECT_NAME, POSTGRES_DB default
backend/pyproject.toml                      ← name field: backend → zabt-backend

# Frontend
frontend-2/package.json                     ← name: frontend-2 → zabt-web
frontend-2/app/login/page.tsx               ← UI text: "Meetily" → "Zabt"
frontend-2/app/register/page.tsx            ← UI text: "Meetily" → "Zabt"

# Governance / Documentation
.specify/memory/constitution.md             ← Title: "Meetily Constitution" → "Zabt Constitution"
CLAUDE.md                                   ← Project name if "Pareto AI" present
README.md (if exists)                       ← Project description

# Spec Documents (historical Logto references to verify/neutralize)
specs/001-migrate-supabase/                 ← verify no active-tense Logto references
```

---

## Complexity Tracking

No constitution violations. This feature is a straight rename with no architectural impact.
