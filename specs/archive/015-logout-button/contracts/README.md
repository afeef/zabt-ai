# Contracts: Logout Button

No API contracts required. This feature is entirely frontend-side.

Logout is handled by `supabase.auth.signOut()` (Supabase JS SDK) — a client-side call that clears session cookies. No backend endpoints are added, modified, or removed.
